<div class = "chat-header">
   <div class = "left-header">
     <div class="header-title">
     <%= @room.name%><!--@roomはcontroller/messages.rbでユーザーに関する値は格納しているのでチャット画面のヘッダーにチャットルーム名が記載される-->
     </div>
   </div>
  <div class="right-header">
    <div class="header-btn">
      <%=link_to "チャットを終了する", room_path(@room), method: :delete%>
    </div>
  </div>
</div>

<div class = "messages">
  <%= render partial: 'message', collection: @messages%>
</div> 


 <%= form_with model: [@room, @message], html: {class: "form"}, local: true do |f| %><!--まず、form_withで@room（指定したチャットルームのレコード情報）,@message（メッセージモデルのインスタンスMessage.new）を引数として渡しているこれはmessagesのルーティングがroomsの子要素になっているから-->
  <div class="form-input">
    <%= f.text_field :content, class: 'form-message',placeholder: 'type a message'%>
    <label class="form-image">
      <span class="image-file">画像</span>
      <%= f.file_field :image, class: 'hidden' %>
    </label>
  </div>
  <%= f.submit '送信', class: 'form-submit' %>
<% end %>
</div>